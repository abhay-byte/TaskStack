---
description: run the flutter app on the connected Android device
---

// turbo-all
1. Check if flutter run is already active; if so, send `r` for hot reload instead of restarting.
2. Run the app on the connected Android device:
```
flutter run -d d30a1726
```
