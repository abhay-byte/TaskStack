import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder widget test', (tester) async {
    // Full widget tests are in integration_test/
    expect(1 + 1, 2);
  });
}
