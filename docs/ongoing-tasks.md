# Ongoing Tasks

## 📱 Phase 7: Flutter Cloud Integration — Auth

We are now moving to the Flutter side to connect to the fully-deployed Node.js backend (hosted on Render).

**Currently in progress:**
- Setting up `dio` HTTP client + `flutter_secure_storage` for token persistence
- Planning `AuthRepository`, `AuthNotifier`, `LoginScreen`, `SignupScreen`
- Adding GoRouter auth guard

**Backend is complete and ready** — see `finished-tasks.md` for details.
